# Validate the excluded class

Helper function to ensure that the excluded level for classification is
an integer. Set to 0L to exclude no class.

## Usage

``` r
validateExcludedClass(arg)
```

## Arguments

- arg:

  The value to check

## Value

integer
